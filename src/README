README

mergeDataLondon.py & mergeDataBeijing.py :
	merge all data in one file for each station.

loadDataLondon.py & loadDataBeijing.py:
	open merged csv for each station and prepare the training, validation and tests sets. 
	(the code is used in predictions*.py files to have the right data to predict).

predictionsLondon.py & predictionsBeijing.py : 
	try the differents predictions techiques on the training and validation tests, computes 	the hyperparamters and the mean squared errors. 

finalModel.py : 
	Compute the final predictions on test data with the best model. 
